[project]
name = "polygeo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lvyuemeng", email = "lv15623448685@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "gpytoolbox>=0.3.7",
    "numpy>=2.2.6",
    "polyscope>=2.6.1",
    "scipy>=1.15.0",
    "trimesh>=4.11.5",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.11",
    "ty>=0.0.32",
]
